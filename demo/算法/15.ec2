算始 是否为完全立方数 (n)
    n := 整数(n)
    若始 (n < 0)
        n := -n
    若终

    i := 1
    当始 (i <= n)
        i3 := i * i * i
        若始 (i3 > n)
            跳出
        若又 (i3 == n)
            输出 (真)
            终止()    # 此时算法必须提前终止
        若终

        i := i + 1  # 别忘了递增，否则会死循环
    当终

    输出 (假)
算终