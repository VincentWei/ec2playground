# n 必须为正整数
算始 是否为完美数 (n)
    # 获取 n 的真因子序列
    n := 整数(n)
    s := []
    i := 1
    当始 (i < n)
        若始 (n % i == 0)
            s := s + [i]
        若终
        i := i + 1
    当终

    # 计算真因子之和
    sum := 0
    i := 0
    当始 (s[i] != 未定义)
        sum := sum + s[i]
        i := i + 1
    当终

    # 判断是否为完美数
    若始 (sum == n)
        输出 (真)
    若否
        输出 (假)
    若终
算终