# 这个算法假设传入的参数 n 是正整数
算始 是否为质数 (n)
    n := 整数(n)
    若始 (n < 2)
        返回 假
    若终

    若始 (n == 2)
        返回 真
    若终

    i := 2
    # 只要 i 的平方大于 n 就没必要继续判断了
    当始 (i * i <= n)
        若始 (n % i == 0)
            返回 假
        若终
        i := i + 1
    当终

    返回 真
算终