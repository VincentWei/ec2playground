算始 最小公倍数 (a, b)
    # 处理 a, b 之一等于零的情况
    a,b := 整数(a),整数(b)
    若始 (a == 0 或 b == 0)
        返回 未定义
    若终

    # 先求出 a, b 的积
    prod := a * b

    # 再求最大公约数
    当始 (b != 0)
        tmp := a
        a := b
        b := tmp % b
    当终

    # 最小公倍数便是积 prod 除以最大公约数的结果
    返回 prod // a
算终