算始 孙子定理朴素解法()
    x := 2

    当始 (x < 1000)

        若始 (x % 3 == 2 且 x % 5 == 3 且 x % 7 == 2)
            返回 x
        若终

        # 符合条件的解一定至少以最小的模数（3）递增
        x := x + 3
    当终

    # 返回 0 表示未找到
    返回 0
算终