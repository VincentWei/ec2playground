算始 数数游戏必胜 (x)
    x := 整数(x)
    若始 (x % 7 == 0)
        执行("拍手")
        终止()
    若否
        y := x
        当始 (y != 0)
            r := y % 10
            若始 (r == 7)
                执行("拍手")
                终止()
            若终
            y := y // 10
        当终
        执行("说出", x)
    若终
算终