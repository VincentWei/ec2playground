算始 自然数转换进制 (x, b)

    若始 (b <= 1)
        返回 未定义
    若终

    s := []
    若始 (x == 0)
        s := [0]
        返回 s
    若终

    当始 (x != 0)
        r := x % b
        s := [r] + s
        x := x // b
    当终

    返回 s
算终
